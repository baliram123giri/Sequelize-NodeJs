<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Activate Your Account Now for Exclusive Benefits!</title>
  <style>
    /* Base styles */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      line-height: 1.5;
    }

    h1, h2 {
      margin-bottom: 1rem;
    }

    p {
      margin-bottom: 0.5rem;
    }

    a {
      color: #333;
      text-decoration: none;
      border-bottom: 1px solid #ddd;
      padding-bottom: 2px;
    }

    a:hover {
      color: #007bff;
    }

    /* Layout */
    .container {
      max-width: 600px;
      margin: 2rem auto;
      padding: 2rem;
      border: 1px solid #ddd;
      border-radius: 4px;
    }

    /* Form styles */
    form {
      display: flex;
      flex-direction: column;
      width: 100%;
    }

    textarea {
      padding: 1rem;
      border: 1px solid #ccc;
      border-radius: 4px;
      resize: vertical;
      margin-bottom: 1rem;
    }

    button[type="submit"] {
      background-color: #007bff;
      color: #fff;
      padding: 0.75rem 1rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.2s ease-in-out;
    }

    button[type="submit"]:hover {
      background-color: #0069d9;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Hello, <%= name %>!</h1>
    <p>Welcome to our platform!</p>
    <p>You recently requested an action on our website: <%= website %>.</p>
    <h2>  <%= comment %></h2>
  </div>
</body>
</html>
